<% if user_signed_in? && @user == current_user %>
  <h1>マイページ</h1>
  <h2>ユーザー名：<%= @user.username %></h2>
  
  <%= link_to "プロフィール編集", edit_user_path(@user.id) %>
  
  <p>自分の投稿一覧</p>
  
  
<% else %>
  <h1>ユーザー詳細ページ</h1>
  <h2>ユーザー名：<%= @user.username %></h2>
  
  <p>ユーザーの投稿一覧</p>
<% end %>

